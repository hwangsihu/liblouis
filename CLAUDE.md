# CLAUDE.md — 한국어 점자 테이블 2024 개정 작업

이 문서는 **2024년 개정 한국 점자 규정**(문화체육관광부 고시 제2024-0005호)에
맞춰 liblouis의 한국어 테이블을 갱신하는 다중 세션 프로젝트의 작업 기록이다.
대화가 초기화되어도 여기서부터 이어서 작업할 수 있도록 유지한다.

## 작업 원칙

- **작은 단위로 검증 가능하게**: 영역(절) 단위로 변경 → 테스트 추가 → 커밋.
- **회귀 방지**: 변경 전 현재 동작을 테스트로 캡처한 뒤 갱신한다 (실패 → 수정 → 통과).
- **테이블 직접 일괄 수정 금지**: 변경점이 명확히 식별된 항목만 수정. 추측으로
  수천 줄을 건드리지 않는다.
- **진행 상황은 이 파일에 누적 기록**: 완료한 작업은 "완료" 섹션으로 옮기고,
  결정·미결 질문은 해당 섹션에 남긴다.
- **작업 단위마다 커밋**: 논리적으로 한 단위가 끝날 때마다 즉시 커밋한다.
  문서·계획 변경과 코드 변경은 별도 커밋. 한 절(節)·한 카테고리 단위로 분리.
  커밋 메시지는 한국어로, 제목은 영문 prefix(`ko:`, `docs(ko):` 등) 사용.
- **주석 최소화**: 코드가 보여주는 WHAT은 주석으로 반복하지 않는다. WHY가
  비자명할 때만 한 줄. 줄별 한국어 라벨(`# 소괄호 ( )`)은 노이즈이므로 금지.
  규정 항(`# 제45항 …`) 인용 주석도 코드/하네스에 넣지 않는다 — 컨텍스트는
  커밋 메시지로 남긴다.
- **장기 가이드는 메모리 대신 CLAUDE.md에 누적**: 이 프로젝트의 결정·규칙은
  세션 간 메모리 파일이 아닌 본 문서에 기록한다.
- **필요 시 이전 채팅 참고**: 본 문서는 multi-session 요약·결정만 담는다.
  시도/회귀의 세부 로그, 사용자 지적의 원문 맥락, 명령 출력 등은 기록하지
  않으니, 모호하거나 누락된 컨텍스트는 이전 대화 로그를 확인한다 (예:
  특정 커밋의 동기, 되돌린 시도의 회귀 양상, 사용자 지정 옵션의 이유).

## 참고 자료 위치

- 규정 본문 PDF: `C:\braille\[개정]+한국+점자+규정+전문.pdf` (7.6 MB)
- 규정 본문 HWPX: `C:\braille\[개정]+한국+점자+규정+전문.hwpx`
  - 추출본: `C:\braille\hwpx_extracted\Contents\section1.xml` (8.8 MB, 본문 전체)
- 규정 점자 BRF (제1·2권): `C:\braille\한국+점자+규정+개정+전문(점자)\`
- 추출 텍스트: `C:\braille\spec.txt` ← `pdftotext` 결과

## 환경/도구

- OS: Windows 11, PowerShell 7
- **빌드 환경**: MSYS2 UCRT64 (`C:\msys64\msys2_shell.cmd -ucrt64`).
  필수 패키지: `mingw-w64-ucrt-x86_64-{gcc,python,pkgconf,libyaml}`.
  - 표준 형식: `C:/msys64/msys2_shell.cmd -ucrt64 -defterm -here -no-start -c "..."`
  - 빌드: `./autogen.sh && ./configure --enable-ucs4 && make -j4`
  - **빌드 전 필수 패치 (미커밋)**: `liblouis/metadata.c`의 `#ifdef _MSC_VER` 두 곳을
    `#if defined(_MSC_VER) || defined(_WIN32)`로 바꿔야 mingw에서 빌드됨
    (gnulib `dirent.h` ↔ mingw native `dirent.h` 충돌 회피, native Win API 사용).
    Working tree에 유지. upstream 보고/PR 후보.
  - 회귀 실행 (PowerShell):
    ```
    $env:PATH = "C:\msys64\ucrt64\bin;" + $env:PATH
    $env:LOUIS_TABLEPATH = "C:\liblouis\tables"
    & "C:\liblouis\tools\.libs\lou_checkyaml.exe" "C:\liblouis\tests\braille-specs\ko-g2_harness.yaml"
    ```
  - DLL 우선순위 주의: Windows가 `C:\Windows\System32\liblouis.dll`(접근성용)을 갖고
    있으므로, `cp liblouis/.libs/liblouis.dll tools/.libs/`로 exe 옆에 둬야 우리 DLL이 로드됨.

## 점자 글꼴 디코드 (필수)

PDF 본문은 **점자 글꼴**로 인쇄되어 ASCII 문자가 점 번호(NABCC 매핑)를 표현.
spec.txt 디코드 시 사용한다.

| 자모/기호 | ASCII | 점 번호 |
|---|---|---|
| ㄱ 첫소리 | `@` | 4 |
| ㄴ 첫소리 | `c` | 14 |
| ㄷ 첫소리 | `i` | 24 |
| ㄹ 첫소리 | `"` | 5 |
| ㅁ 첫소리 | `e` | 15 |
| ㅂ 첫소리 | `^` | 45 |
| ㅅ 첫소리 | `,` | 6 |
| ㅇ 첫소리 | (생략) / `g`로 표시 시 1245 | (다만2) |
| ㅈ 첫소리 | `.` | 46 |
| ㅊ 첫소리 | `;` | 56 |
| ㅋ 첫소리 | `f` | 124 |
| ㅌ 첫소리 | `h` | 125 |
| ㅍ 첫소리 | `d` | 145 |
| ㅎ 첫소리 | `j` | 245 |
| ㄱ 받침 | `a` | 1 |
| ㄴ 받침 | `3` | 25 |
| ㄷ 받침 | `9` | 35 |
| ㄹ 받침 | `1` | 2 |
| ㅁ 받침 | `5` | 26 |
| ㅂ 받침 | `b` | 12 |
| ㅅ 받침 | `'` | 3 |
| ㅇ 받침 | `7` | 2356 |
| ㅈ 받침 | `k` | 13 |
| ㅊ 받침 | `2` | 23 |
| ㅋ 받침 | `6` | 235 |
| ㅌ 받침 | `8` | 236 |
| ㅍ 받침 | `4` | 256 |
| ㅎ 받침 | `0` | 356 |
| ㅏ | `<` | 126 |
| ㅑ | `>` | 345 |
| ㅓ | `s` | 234 |
| ㅕ | `:` | 156 |
| ㅗ | `u` | 136 |
| ㅛ | `+` | 346 |
| ㅜ | `m` | 134 |
| ㅠ | `%` | 146 |
| ㅡ | `[` | 246 |
| ㅣ | `o` | 135 |
| ㅐ | `r` | 1235 |
| ㅔ | `n` | 1345 |
| 된소리표 | `,` (앞쪽) | 6 |
| 온표 | `=` | 123456 |
| 수표 | `#` | 3456 |

(나머지 NABCC 매핑은 `tables/braille-patterns.cti` 참고.)

## 대상 파일

| 파일 | 크기 | 역할 |
|---|---|---|
| `tables/ko.cti` | 작음 | 기본 헤더, 문장 부호, indicator |
| `tables/ko-chars.cti` | 388 KB | 한글 음절(자모/완성형) → 점자 매핑 |
| `tables/ko-g1-rules.cti` | 633줄 | G1: 수 뒤 한글 처리 (간격 dot 0 삽입) |
| `tables/ko-g2-rules.cti` | 277 KB | G2: 약자/약어/축약, 수 뒤 한글 처리 |
| `tables/ko-g1.ctb`, `ko-g2.ctb` | 작음 | 위 파일들을 묶는 진입점 |
| `tests/braille-specs/ko-g2_harness.yaml` | — | 자동 테스트 |

## spec.txt 절별 위치 인덱스 (행 번호)

| 절 | 시작 행 | 비고 |
|---|---|---|
| 한국 점자 표기의 기본 원칙 | ~421 | |
| 제1절 첫소리로 쓰인 자음자 | 477 | |
| 제2절 받침으로 쓰인 자음자 | 513 | |
| 제3절 모음자 | 559 | |
| 제4절 단독으로 쓰인 자모 | 587 | |
| 제5절 모음 연쇄 | 664 | 제11항(예 구분표), 제12항(애 구분표) |
| **제6절 약자** | 703 | G2 영향 큼. 제13·14·15·16·17항 |
| **제7절 약어** | 813 | G2 영향 큼 |
| 제8절 옛 자음자 | 864 | |
| 제9절 옛 모음자와 방점 | 1045 | |
| 제10절 국어 문장 안의 로마자/그리스 | 1126 | 제29·32항 (전환 마커) |
| 제11절 국어 문장 안의 숫자 | 1436 | |
| 제12절 숫자와 함께 사용된 기호 | 1522 | 제45·46·47·48항 |
| **제13절 문장 부호** | 1592 | 사용자 우선순위 |
| **제14절 기타 부호** | 1959 | 제64·65·67·68·69·70·71·72항 |
| 수학 점자 (제1~9장) | 4796~ | 후순위 |
| 음악 점자 (정간보·오선보) | 4796~6386 | 후순위 |
| 외국어 점자 표기 | 8617~ | 후순위 |

## 단계별 계획

- [x] **Phase 0** 환경 준비: Poppler 설치, `spec.txt` 추출, 절별 행 인덱스.
- [~] **Phase 1** 변경점 카탈로그: 제5·12·13·14절, 제64·65·67·69항 완료.
      약자/약어 detail, 옛글자, 로마자 전환, 수학·음악·외국어 미착수.
- [~] **Phase 2** 테스트 하네스: 104개 회귀 테스트 누적. MSYS2 UCRT64 빌드
      환경 구축 완료, 전건 통과(0 failures). G1 하네스는 미작성.
- [~] **Phase 3** 영역별 수정: 아래 "구현 진행" 표 참조.
- [x] **Phase 4** 정리: `ko-2006*` 파일·하네스 삭제, 빌드 시스템 정리,
      ctb 헤더 TODO/FIXME 제거, 저작권 갱신.
- [ ] **Phase 5** (막바지) 언어 전환 마커, 쌍시옷·예 충돌 점검, 한소네 입력
      테이블 신설 — 아래 "막바지 작업" 섹션 참고.

## 구현 진행 (2024 spec 기준)

| 항목 | 상태 | 메모 |
|---|---|---|
| 제11항 모음+예 구분표 36 | ✅ | 252개 엔트리 일괄 보정 (4558c787) + 가예~가옣 28개 추가 |
| 제14항 [붙임] 팠 ㅏ 생략 금지 | ✅ | 145-34 → 145-126-34 |
| 제16항 [붙임] 껐 매핑 정정 | ✅ | 6-456-234-3(된소리+것+ㅅ) → 6-4-234-34(꺼+ㅆ) |
| 제17항 성/썽/정/쩡/청 | ✅ | spec 일치 확인, 변경 없음 |
| 제15항 약자 단음절 14개 | ✅ | spec 일치 확인 (noback always), 회귀 테스트 14건 추가 |
| 제15항 [붙임] 표본 (덕/꺾/얹/얽) | ✅ | spec 일치, 회귀 4건 추가 |
| 제16항 까/싸 단음절 | ✅ | spec 일치, 회귀 2건 추가 |
| 제13·14절 문장부호·기타 | ✅ | f4ae9bc1 |
| 제45항 ×, ÷ | ✅ | 6c3d3eb4 |
| 제64항 동그라미 숫자 ①~⑳ | ✅ | 511b6252 |
| 제64항 동그라미 로마자 ⓐ~ⓩ | ✅ | 64367f40 |
| 제64항 동그라미 한글 자음 ㉠~㉭ | ✅ | 28c76db1 |
| 제64항 동그라미 한글 음절 ㉮~㉻ | △ | 약자형으로 통일 등록(3ab40732). G1 분리 미해결 |
| 제64항 네모 문자 | ❌ | Unicode 매핑 표준 없음 |
| 제65항 화폐 (₩ € ￡ ￠ ₣ ￥) | ✅ | f0bddd13. `begnum $ 256` → always-only |
| 제67항 점형표 `_=` | ✅ | 80f8653c |
| 제68항 첨자 (위/아래) | ❌ | ASCII `^`/`;`와 충돌. 입력 형식 결정 필요 |
| 제69항 비로마자 단위 (%, °, ℃ 등) | ✅ | 4d3bd77d |
| 제69항 SI 단위 (㎏ ㎡ ㏊ 등) | ✅ | 84f9fb7c 21개 추가. 빗금 인접 단위 wrapping 정리(64247757) — pass2 cell 룰 |
| 제53항 줄임표 (… ...) | ✅ | … → 6-6-6, ... → 256-256-256. 영문 G1 override 차단(56477875) |
| 제6·7절 약자/약어 회귀 검증 | ❌ | spec 차이는 제11항 외 별도 정밀 검증 필요 |
| 제8·9절 옛 글자 | ❌ | 후순위 |
| 제10절 로마자/그리스 | ❌ | 후순위 + Phase 5 전환 마커 |
| 수학·음악·외국어 | ❌ | 후순위 |

## 알려진 잔여 이슈

### 가 약자 + 모음 시리즈 (제14항 해석 주의)

- 제14항 본문은 **'나, 다, 마, 바, 자, 카, 타, 파, 하'**만 명시 — **가는 제외**.
  따라서 가에 모음이 붙어도 **약자(1246) 그대로 사용** (예: `가옥 = $x = 1246+옥`).
- 그러나 가예~가옣은 제11항(모음+예 → 구분표 36)이 적용되어야 함.
  현재 동작은 1246+34(예)로 36 누락 → 제11항 위반.
- 가예~가옣 28개 엔트리는 1246-36-34 패턴으로 추가 (풀어쓰기 아님).

### 제68항 첨자 입력 형식

- 위 첨자 `^`(45) / 아래 `;`(56)이 ASCII 입력 그대로면 ko.cti의 `always ^ 4-26`,
  `postpunc ; 56-23`과 충돌. 입력 표기 결정 필요(예: `^`-회피 또는 별도 마커).

### `;` `:` `?` 영어식 매핑

- 현 ko.cti의 `postpunc ; 56-23` 등이 영어 컨벤션과 우연히 일치.
  변경 시 영문 처리 부작용 검토.

### `ko-g1.ctb`의 영문 약자

- ko-g1.ctb가 `include en-us-g2.ctb` 한 적 없음 (현 시점). g1-rules가
  G2 약자를 끌어오지는 않음. 단, ko-g2-rules.cti는 `include en-us-g2.ctb`.

### 영문 G1 부호 override (구조 미해결, case-by-case 처리 중)

- 로드 순서: ko.cti → ko-g2-rules.cti(en-us-g2.ctb 포함→ en-us-g1.ctb).
  영문 G1이 마지막 → ko.cti의 한국 부호 매핑(`always ... 256-256-256` 등)
  을 후행 정의가 override할 수 있음.
- spec 제32항(영자 부분은 통일영어점자)으로 인해 영문 G2 include는 유지
  필요 — include 자체는 제거하지 않음.
- **결정 (2026-05-05)**: 옵션 C 채택. 충돌 발견된 항목만 ko-g2-rules.cti의
  en-us-g2.ctb include 직후에 한국 spec 매핑 재선언. 처리 완료: `...`,
  `..."`, `...'` (56477875).
- 향후 다른 부호 충돌 발견 시 같은 패턴(en-us include 직후 재선언)으로 처리.
- 근본 개편 옵션 B(ko.cti를 부호/한글로 분리, ko-g2.ctb include 순서 변경)
  는 미적용 — 영향 범위 크고 검증 비용 높음.

## 막바지 작업 (Phase 5)

다른 영역 작업 마무리 후 마지막 단계로 진행. 외부 사용자 피드백 반영분.
**작업 종료 시 이 섹션은 삭제**한다 (CLAUDE.md를 짧게 유지).

### A. 한글↔로마자 전환 마커 (제29·32항)

**관련 spec 조항** (spec.txt L1126~1340):
- 제29항: 영자 그룹 앞에 로마자표 0(=356), 뒤에 종료표 4(=256)
- 제29항 [다만]: 문단 전체가 로마자일 때 시작/종료표 생략 가능
- 제33항: 영자↔한글 사이 점형이 다른 부호(`, : ; ―`) → 종료표 생략, 부호는 한글식
- 제33항 [다만]: `. ? ! ...` 뒤 종료표 생략 / `/ - ~` 앞에 종료표
- 제34항: 따옴표·괄호 안 영자 → 종료표 생략
- 제35항: 영자와 숫자 이어 나올 때 → 종료표 생략

**현재 동작 진단 (2026-05-04)**:
- `letsign 356`은 ko.cti에 정의돼 있으나 ko-g2-rules.cti가 include하는
  en-us-g2.ctb → en-us-g1.ctb의 `letsign 56`이 더 늦게 로드돼 override.
  결과적으로 G2에서 letsign이 56(점 5-6)으로 출력 (한국 spec 위반).
- 자동 letsign은 단일 letter에만 적용, 다중 letter 그룹·종료표는 미구현.

**시도 (2026-05-04)와 회귀**:
ko-g2-rules.cti에 다음 추가:
```
noletsign abcdefghijklmnopqrstuvwxyz
noletsign ABCDEFGHIJKLMNOPQRSTUVWXYZ
noback context `[]$l @356           # 입력 시작 + letter
noback context $s[]$l @356          # space + letter
noback context $S[]$l @356          # sign(한글 등) + letter
noback context $p[]$l @356          # punct + letter
noback context $l[]~ @256            # letter + 입력 끝
noback context $l[]$s @256          # letter + space
noback context $l[]$S @256          # letter + sign
```
한↔영 기본 경계는 정확히 작동(`안녕 hi 2025` → `⠣⠒⠉⠻ ⠴⠓⠊⠲ ⠼⠃⠚⠃⠑`).
그러나 회귀 3건:
- `unity` 단독 → 시작 356 추가 (제29항 [다만] 미반영. 사용자 결정 필요)
- `@a` → `@` 점형이 ⠈→⡈로 7점 변형 (사이드 이펙트, 미규명)
- `1 %p` → `%p`(ko.cti `always %p 356-1234-1234`) 매핑 분해.
  context의 `$l[]$s @256`이 단독 'p'에 매치돼 multi-char 매핑 우선순위 깨짐.

또 영자 단어 내에 contraction(`ne`, `st` 등)이 적용된 경우 마지막 letter가
contraction에 흡수돼 종료표 매치 실패(예: `iPhone` 끝에 256 누락).

**다음 시도 시 고려할 방향**:
1. context 룩어헤드를 `$l$l[]$s` 같이 다중 letter 한정해 단일 letter는
   letsign에 위임 — 단 `%p` 같은 multi-char 매핑은 여전히 충돌 우려.
2. pass2 cell-level 후처리 — 영자 점형(8점 letter) vs 한글 점형 식별 어려움.
3. 단어 단위 매칭 — `prfword`/`sufword` 같은 단어 경계 opcode 활용 검토.
4. en-us-g2.ctb가 정의하는 contraction 룰이 letter 매치를 가로채는 문제는
   context 룰을 pass2/pass3로 옮기는 식으로 회피 가능 여부 확인.

**한계**: 순방향 전용. 역변환은 별도 처리.

### B. 쌍시옷 받침 vs. 예 충돌 (제14·16항 영역)

- spec 사실: ㅆ받침 = 점 34, ㅖ "예" = 점 34 (동일 점형).
- spec disambiguation: 제14항 본문(자음자 약자 + 모음 → 약자 미사용),
  **제14항 [붙임]** "팠은 ㅏ를 생략하지 않는다" — **ㅍ만 명시 예외**.
  ㄴ·ㄷ·ㅁ·ㅂ·ㅈ·ㅋ·ㅌ·ㅎ에 대한 [붙임] 명시는 spec.txt에 없음.
- 사용자 언급 "36점 부가" 규칙은 spec.txt 검색 안 됨 — **출처 확인 필요**
  (점역 지침서 등 외부 자료?). 결정되면 `ko-g2-rules.cti`에 컨텍스트 규칙 추가.

### C. 한소네 8점 입력 backward 테이블 (신규)

- 목적: 한소네에서 8점 점자 시퀀스로 입력한 문장부호를 텍스트로 역변환.
- 가능 여부: **가능**. `nofor pass2 @236-38 @238` 형식으로 다중 셀 → 단일 셀
  축약. 동일 패턴이 `tables/th-comp8-backward.utb`에 이미 사용됨.
- 신설 파일: `tables/ko-hansone-comp8-backward.utb`
  - 헤더에 `#+direction: backward` 명시
  - 모든 규칙에 `nofor` 접두사
- 주의: prefix 충돌 방지를 위해 longest-match 설계. dot 7·8은 한소네
  펌웨어가 엔터/백스페이스로 해석하므로 점자 셀로는 입력 안 됨 (테이블 범위 밖).

#### 한소네 18.2.1 한글 문장 부호 입력 매핑 (전체)

> ※ 8점은 엔터, 7점은 백스페이스 — 점자 셀로는 입력 안 됨.
> 각 항목은 입력 시 누르는 점자 셀 시퀀스. 작업 종료 시 이 표는 본 문서에서
> 삭제하고 `tables/ko-hansone-comp8-backward.utb`에 옮긴다.

| 묵자 | 입력 시퀀스 |
|---|---|
| 여는 소괄호 `(` | 2-3-6-8 → 3-8 |
| 닫는 소괄호 `)` | 6-8 → 3-5-6-8 |
| 여는 중괄호 `{` | 2-3-6-8 → 2-8 |
| 닫는 중괄호 `}` | 5-8 → 3-5-6-8 |
| 여는 대괄호 `[` | 2-3-6-8 → 2-3-8 |
| 닫는 대괄호 `]` | 5-6-8 → 3-5-6-8 |
| 여는 겹낫표 `『` | 5-6-8 → 2-3-6-8 |
| 닫는 겹낫표 `』` | 3-5-6-8 → 2-3-8 |
| 여는 홑낫표 `「` | 5-8 → 2-3-6-8 |
| 닫는 홑낫표 `」` | 3-5-6-8 → 2-8 |
| 여는 겹화살괄호 `《` | 5-6-8 → 2-3-5-6-8 |
| 닫는 겹화살괄호 `》` | 2-3-5-6-8 → 2-3-8 |
| 여는 홑화살괄호 `〈` | 5-8 → 2-3-5-6-8 |
| 닫는 홑화살괄호 `〉` | 2-3-5-6-8 → 2-8 |
| 여는 큰따옴표 `"` | 2-3-6-8 |
| 닫는 큰따옴표 `"` | 3-5-6-8 |
| 여는 작은따옴표 `'` | 6-8 → 2-3-6-8 |
| 닫는 작은따옴표 `'` | 3-5-6-8 → 3-8 |
| 붙임표 `-` | 3-6 |
| 빗금 `/` | 3-4-8 |
| 마침표 `.` | 2-5-6-8 |
| 쉼표 `,` | 5-8 |
| 별표 `*` | 5-8 → 3-5-8 |
| 느낌표 `!` | 2-3-5-8 |
| 물음표 `?` | 2-3-6-7 |
| 쌍점 `:` | 5-8 → 2-8 |
| 세미콜론 `;` | 5-6-8 → 2-3-8 |
| 가운뎃점 `·` | 5-8 → 2-3-8 |
| 물결표 `~` | 4-8 → 3-5-8 |
| 줄임표 `…` | 2-5-6-8 → 2-5-6-8 → 2-5-6-8 |
| 샵 `#` | 3-4-5-6-8 |
| 달러 `$` | 1-2-4-6-8 |
| 퍼센트 `%` | 1-4-6-8 |
| 앤드 `&` | 4-8 → 1-2-3-4-6-8 |
| 오른쪽 화살표 `→` | 2-5-8 → 1-3-5-7 |
| 왼쪽 화살표 `←` | 2-4-6-8 → 2-5-8 |
| 등호 `=` | 2-5-8 → 2-5-8 |
| 플러스 `+` | 2-6-8 |
| 마이너스 `-` | 3-5-7 |
| 레스 댄 `<` | 1-2-6-8 |
| 그레이터 댄 `>` | 3-4-5-8 |
| 백슬래시 `\` | 1-6-8 |
| 수직바 `|` | 1-2-5-6-8 |
| 캐럿 `^` | 4-8 → 2-6-8 |
| 언더바 `_` | 4-5-6-7 |
| 액센트 `` ` `` | 4-7 |
| 골뱅이 `@` | 4-8 |
| 어퍼스트로피 `'` | 3-7 |

## 결정/미결 질문

- ✅ **결정 (2026-05-03)**: 우선순위는 기호·문장부호(제12·13·14절) → 약자/약어 순.
- _Q_ **(외부 피드백, 2026-05-04)**: 자음자 약자(ㄴ·ㄷ·ㅁ·ㅂ·ㅈ·ㅋ·ㅌ·ㅎ)에서
  쌍시옷 받침/예 충돌을 "36점 부가"로 회피한다는 관행의 출처. spec 본문에는
  없음 — 점역 지침서 등 외부 자료 인용 확인 필요.

## 진행 상황 (날짜순, 최신이 위)

### 2026-05-13
- **Phase 4 정리 완료**: `ko-2006*` 테이블 3종(`ko-2006.cti`,
  `ko-2006-g1.ctb`, `ko-2006-g2.ctb`) + 하네스(`ko-2006-g2_harness.yaml`)
  삭제. 빌드 시스템(`tables/Makefile.am`, `tests/Makefile.am`,
  `tests/braille-specs/Makefile.am`)·`spaces.yaml`·`fuzzing.yml`에서
  ko-2006 참조 제거. ko-chars.cti 헤더 주석에서 "old set/new 2006
  edition" 문구를 단일 ko.cti 참조로 갱신. `ko-g1.ctb`/`ko-g2.ctb`
  헤더의 metadata TODO 제거, 2024 spec 참조 추가, 저작권 라인 갱신.
  `ko-g2.ctb`의 stale FIXME(emphasis class 중복) 제거 — 실제 검증 결과
  ko-g2-rules.cti에는 emphasis class 정의 없음.
  회귀 ko-g2 104건 + spaces 1531건 통과.
- **CLAUDE.md 정리**: 환경/도구에서 spec.txt 추출 완료된 Poppler/`pdftotext`
  관련 줄 제거. msys2_shell 호출 가이드를 `-no-start` 사용으로 변경
  (창 깜빡임 방지, 사용자 재테스트 결과).

### 2026-05-05
- **제53항 줄임표 영문 G1 override 차단** (56477875):
  사용자 지적("…은 6-6-6, ...은 256-256-256")으로 검증 중 발견된 충돌.
  en-us-g1.ctb의 `always ... 3-3-3`, `repeated .... 0`이 ko.cti의 한국
  spec 매핑을 후행 override하던 문제. ko-g2-rules.cti의 en-us-g2.ctb
  include 직후에 한국 spec 매핑 3건(`... ..." ...'`) 재선언으로 차단.
  ko.cti의 영문 잔재 (`repeated .... 0`, `.\s.\s. 3-3-3` 2건) 삭제.
  회귀 테스트 2건 추가. 영문 부호 override 구조 문제는 잔여 이슈로 등록
  (옵션 C 채택, case-by-case 처리).
- **제69항 인접 단위 wrapping 정리** (64247757):
  ko.cti에 cell-level pass2 룰 `@256-456-34-356 @456-34` 한 줄 추가.
  `0mg4_/0dl4` (10셀) → `0mg_/dl4` (8셀, spec 일치). 회귀 테스트 4건 추가.
  면적·부피 단위(㎟ ㎠ ㎡ 등) 끝 종료표 부재는 spec L2071 명시 패턴
  (`0m^#b`)이라 변경 없음 — 숫자 첨자 #b 뒤 4 모호 회피.
- **Phase 5 A 한↔영 전환 시도와 회귀** (4d537db0 — 미커밋 변경분 되돌림):
  ko-g2-rules.cti에 `noletsign` + `noback context $S/$s/$p[]$l @356`
  +  `$l[]$s/$S/~ @256` 8건 시도. 한↔영 기본 경계는 작동
  (`안녕 hi 2025` → `⠣⠒⠉⠻ ⠴⠓⠊⠲ ⠼⠃⠚⠃⠑`)하나 회귀 3건 발생:
  `unity` 단독에 시작표 추가, `@a` 점형 7점 변형, `1 %p` 매핑 분해.
  사용자 지시로 변경분 되돌림. 분석은 Phase 5 A 섹션에 누적.

### 2026-05-04
- **기존 회귀 실패 9건 전부 해결** (6434d75a, fcb810a9, 91fa3d6c):
  - ko.cti: 컬리 쿼트(`‘ ’ “ ”`) 한국 점자 매핑 4건 추가. en-chardefs.cti의
    `noback punctuation \x2019 3`이 spec(닫는 작은따옴표 = 356-3)과 충돌
    하던 문제 해결.
  - 하네스 expected 8건 정정 + xfail 2건 제거. 모두 작성 오류 (예=34인데
    ⠝(1345=에)으로 기록, 음=246-26인데 ⠭(1346)으로 기록, 끝 "나" 단독에
    14-126 적용 등). 사용자 지적 "닫는 작은따옴표는 3점이 아니야"가 ko.cti
    누락 발견 계기.
- **제15·16·17항 회귀 보강** (188a3596):
  단음절 약자 14개(억/언/얼/연/열/영/옥/온/옹/운/울/은/을/인) + [붙임] 4개
  (덕/꺾/얹/얽) + 까/싸 + 성/썽/정/쩡/청 모두 spec 일치 확인, 회귀 테스트
  28건 추가, 코드 변경 없음.
- **제16항 [붙임] '껐' 정정** (4be4726b): 6-456-234-3(된소리+것+ㅅ받침으로
  잘못 해석) → 6-4-234-34(꺼+ㅆ받침). spec 점자 예시 `,@s/` 일치.
- **제14항 [붙임] '팠' 예외 적용** (f6e10379): 145-34 → 145-126-34
  (ㅏ 생략 금지). 나머지 -았 시리즈는 spec [붙임] 비명시라 유지.
- **가예~가옣 28개 추가** (4a002146, 1246-36-34 패턴). 제14항이 '가'를
  명시 제외하므로 약자(1246) 그대로 사용 + 제11항 36 구분표.
- **제11항 모음+예 구분표 36 일괄 보정** (4558c787): 252개 엔트리 정정.
- **ko.cti / ko-chars.cti**: 제45·64·65·67·69항 매핑 추가/정정.
  6c3d3eb4, f0bddd13, 4d3bd77d, 80f8653c, 511b6252, 64367f40, 28c76db1,
  84f9fb7c, 3ab40732.
- **MSYS2 UCRT64 빌드 환경 구축**. `lou_checkyaml`로 회귀 실행 가능.
- 외부 피드백 3건 접수 → Phase 5로 등록: 한글↔로마자 전환 마커, 쌍시옷·예
  충돌, 한소네 8점 입력 표.

### 2026-05-03
- Phase 0 완료. Poppler 설치, spec.txt 추출, 절별 행 인덱스.
- **제13·14절 문장 부호·기타 부호 ko.cti 갱신** (f4ae9bc1). 하네스 예제 25건.
