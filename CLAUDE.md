# CLAUDE.md — 한국어 점자 테이블 2024 개정 작업

이 문서는 **2024년 개정 한국 점자 규정**(문화체육관광부 고시 제2024-0005호)에
맞춰 liblouis의 한국어 테이블을 갱신하는 다중 세션 프로젝트의 작업 기록이다.
대화가 초기화되어도 여기서부터 이어서 작업할 수 있도록 유지한다.

## 작업 원칙

- **작은 단위로 검증 가능하게**: 영역(절) 단위로 변경 → 테스트 추가 → 커밋.
- **회귀 방지**: 변경 전 현재 동작을 테스트로 캡처한 뒤 갱신한다 (실패 → 수정 → 통과).
- **테이블 직접 일괄 수정 금지**: 변경점이 명확히 식별된 항목만 수정. 추측으로
  수천 줄을 건드리지 않는다.
- **진행 상황은 이 파일에 누적 기록**: 완료한 작업은 "완료" 섹션으로 옮기고,
  결정·미결 질문은 해당 섹션에 남긴다.
- **작업 단위마다 커밋**: 논리적으로 한 단위가 끝날 때마다 즉시 커밋한다.
  문서·계획 변경과 코드 변경은 별도 커밋. 한 절(節)·한 카테고리 단위로 분리.
  커밋 메시지는 한국어로, 제목은 영문 prefix(`ko:`, `docs(ko):` 등) 사용.
- **주석 최소화**: 코드가 보여주는 WHAT은 주석으로 반복하지 않는다. WHY가
  비자명할 때만 한 줄. 줄별 한국어 라벨(`# 소괄호 ( )`)은 노이즈이므로 금지.

## 참고 자료 위치

- 규정 본문 PDF: `C:\braille\[개정]+한국+점자+규정+전문.pdf` (7.6 MB)
- 규정 본문 HWPX: `C:\braille\[개정]+한국+점자+규정+전문.hwpx`
  - 추출본: `C:\braille\hwpx_extracted\Contents\section1.xml` (8.8 MB, 본문 전체)
- 규정 점자 BRF (제1·2권): `C:\braille\한국+점자+규정+개정+전문(점자)\`
- 추출 텍스트(생성 예정): `C:\braille\spec.txt` ← `pdftotext` 결과

## 환경/도구

- OS: Windows 11, PowerShell 7
- 설치된 도구: `scoop`, `winget`, Python 3.14 (`py`, `python`), **Poppler** (`pdftotext` 25.12.0)
- 추출 명령: `pdftotext -layout -enc UTF-8 'C:\braille\[개정]+한국+점자+규정+전문.pdf' 'C:\braille\spec.txt'`

## 점자 글꼴 디코드 (필수)

PDF 본문은 **점자 글꼴**로 인쇄되어, ASCII 문자 = 점 번호(NABCC 매핑) 형태로 보인다.
spec.txt에서 점자 예시(`@s"o` 등)를 해석할 때 사용한다.

| 자모/기호 | ASCII | 점 번호 |
|---|---|---|
| ㄱ 첫소리 | `@` | 4 |
| ㄴ 첫소리 | `c` | 14 |
| ㄷ 첫소리 | `i` | 24 |
| ㄹ 첫소리 | `"` | 5 |
| ㅁ 첫소리 | `e` | 15 |
| ㅂ 첫소리 | `^` | 45 |
| ㅅ 첫소리 | `,` | 6 |
| ㅇ 첫소리 | (생략) / `g`로 표시 시 1245 | (다만2) |
| ㅈ 첫소리 | `.` | 46 |
| ㅊ 첫소리 | `;` | 56 |
| ㅋ 첫소리 | `f` | 124 |
| ㅌ 첫소리 | `h` | 125 |
| ㅍ 첫소리 | `d` | 145 |
| ㅎ 첫소리 | `j` | 245 |
| ㄱ 받침 | `a` | 1 |
| ㄴ 받침 | `3` | 25 |
| ㄷ 받침 | `9` | 35 |
| ㄹ 받침 | `1` | 2 |
| ㅁ 받침 | `5` | 26 |
| ㅂ 받침 | `b` | 12 |
| ㅅ 받침 | `'` | 3 |
| ㅇ 받침 | `7` | 2356 |
| ㅈ 받침 | `k` | 13 |
| ㅊ 받침 | `2` | 23 |
| ㅋ 받침 | `6` | 235 |
| ㅌ 받침 | `8` | 236 |
| ㅍ 받침 | `4` | 256 |
| ㅎ 받침 | `0` | 356 |
| ㅏ | `<` | 126 |
| ㅑ | `>` | 345 |
| ㅓ | `s` | 234 |
| ㅕ | `:` | 156 |
| ㅗ | `u` | 136 |
| ㅛ | `+` | 346 |
| ㅜ | `m` | 134 |
| ㅠ | `%` | 146 |
| ㅡ | `[` | 246 |
| ㅣ | `o` | 135 |
| ㅐ | `r` | 1235 |
| ㅔ | `n` | 1345 |
| 된소리표 | `,` (앞쪽) | 6 |
| 온표 | `=` | 123456 |
| 수표 | `#` | 3456 |

(나머지 NABCC 매핑은 `tables/braille-patterns.cti` 참고.)

## 대상 파일

| 파일 | 크기 | 역할 |
|---|---|---|
| `tables/ko.cti` | 작음 | 기본 헤더, 문장 부호, indicator |
| `tables/ko-chars.cti` | 388 KB | 한글 음절(자모/완성형) → 점자 매핑 |
| `tables/ko-g1-rules.cti` | 633줄 | G1: 수 뒤 한글 처리 (간격 dot 0 삽입) |
| `tables/ko-g2-rules.cti` | 277 KB | G2: 약자/약어/축약, 수 뒤 한글 처리 |
| `tables/ko-g1.ctb`, `ko-g2.ctb` | 작음 | 위 파일들을 묶는 진입점 |
| `tests/braille-specs/ko-g2_harness.yaml` | — | 유일한 자동 테스트 (개정 기준 X) |

비교 참고용으로 보존: `tables/ko-2006*.{cti,ctb}` (구 2006 판). 수정 금지.

## spec.txt 절별 위치 인덱스 (행 번호)

| 절 | 시작 행 | 비고 |
|---|---|---|
| 한국 점자 표기의 기본 원칙 | ~421 | |
| 제1절 첫소리로 쓰인 자음자 | 477 | |
| 제2절 받침으로 쓰인 자음자 | 513 | |
| 제3절 모음자 | 559 | |
| 제4절 단독으로 쓰인 자모 | 587 | |
| 제5절 모음 연쇄 | 664 | |
| **제6절 약자** | 703 | G2 영향 큼 |
| **제7절 약어** | 813 | G2 영향 큼 |
| 제8절 옛 자음자 | 864 | |
| 제9절 옛 모음자와 방점 | 1045 | |
| 제10절 국어 문장 안의 로마자/그리스 문자 | 1126 | |
| 제11절 국어 문장 안의 숫자 | 1436 | |
| 제12절 숫자와 함께 사용된 기호 | 1522 | |
| **제13절 문장 부호** | 1592 | 사용자 우선순위 |
| **제14절 기타 부호** | 1959 | 사용자 우선순위 |
| 수학 점자 (제1~9장) | 4796~ | 후순위 |
| 음악 점자 (정간보·오선보) | 4796~6386 | 후순위 |
| 외국어 점자 표기 | 8617~ | 후순위 |

## 단계별 계획

### Phase 0 — 환경 준비 ✅
- [x] 사용자가 `scoop install poppler` 실행
- [x] `pdftotext` 로 본문 추출 → `C:\braille\spec.txt` (320KB)
- [x] 목차 식별, 절별 행 번호 매핑

### Phase 1 — 변경점 카탈로그 작성
2024 개정안에서 2006판 대비 바뀐 항목을 목록화한다. 결과는 이 문서의
"변경점 목록" 섹션에 누적.
- [ ] 제1장 자모와 약자 (한글 자모, 약자 ㄱ~ㅎ 36개, 약어 7개 등)
- [ ] 제2장 단어와 문장 표기
- [ ] 제3장 문장 부호 ← **표시 규칙 영향 큼**
- [ ] 제4장 숫자와 영어
- [ ] 제5장 수학·과학 기호
- [ ] 제6장 기타 기호 ← **기호 영향 큼**

### Phase 2 — 테스트 하네스 보강
- [ ] `tests/braille-specs/ko-g2_harness.yaml`에 개정 규정의 예문(가능하면 BRF
      파일과 대조 가능한 것)을 추가하여 현재 출력과 비교 → 실패 케이스 목록화.
- [ ] G1용 테스트 하네스(`ko-g1_harness.yaml`) 신규 작성 검토.

### Phase 3 — 영역별 수정
한 절씩 PR 단위로:
1. 변경점 카탈로그에서 한 절 선택
2. 해당 절 관련 테스트 케이스 추가 (현재 실패)
3. 테이블 수정 → 테스트 통과
4. 회귀 테스트(`make check` 또는 `lou_checktable`) 통과 확인
5. 진행 상황 기록 → 완료 섹션으로 이동

### Phase 4 — 메타데이터/문서 정리
- [ ] `ko-g1.ctb`, `ko-g2.ctb` 헤더의 TODO·메타데이터 갱신 (출처를 2024 개정으로)
- [ ] 저작권 연도 갱신, 변경 이력 추가

## 변경점 목록 (Phase 1 결과 누적)

각 항목 형식: `기호 — 규정 점형(2024) | 현재 ko.cti 점형 | 비고`.
"누락"은 현재 테이블에 매핑이 없거나 한국 표준에 부합하지 않게 영어식으로 적힌 경우.

### 제13절 문장 부호 (제49항~제58항) — spec.txt L1592–1958

**기존 매핑 수정 필요 (현재 liblouis가 규정과 다름)**

| 묵자 | 규정 | 현재 ko.cti | 메모 |
|---|---|---|---|
| `(` 여는 소괄호 | 236-3 | `prepunc ( 36` | 현재 잘못됨 |
| `)` 닫는 소괄호 | 6-356 | `postpunc ) 36` | 현재 잘못됨 |
| `{` 여는 중괄호 | 236-2 | `prepunc { 236-23` | |
| `}` 닫는 중괄호 | 5-356 | `postpunc } 56-356` | |
| `[` 여는 대괄호 | 236-23 | `prepunc [ 236-3` | () 매핑과 뒤바뀜 |
| `]` 닫는 대괄호 | 56-356 | `postpunc ] 6-356` | |
| `!` 느낌표 | 235 | `postpunc ! 456` | 현재 잘못됨 |
| `*` 별표/참고표 | 5-35 | `always * 35-35` | 약물기호와 통합 (제60항) |
| `^` | 4-26 | `always ^ 456-126` | (제71항) |
| `\` | 456-16 | `always \\ 4-1256` | (제71항) |
| `_` 아래선 | 456-36 | `always _ 6-36-36-3` | (제71항). 현재 4점 반복 잘못됨 |
| `…` 줄임표 | 6-6-6 | `always … 5-5-5` | 현재 5-5-5 잘못됨 |
| `...` 마침표형 줄임표 | 256-256-256 | `always ... 5-5-5` | (제53항) |
| `—` 줄표 (em dash) | 36-36 | `always — 56-36-36-23` | (제55항) |

**신규 추가 필요 (현재 매핑 없음)**

| 묵자 | 규정 | 비고 |
|---|---|---|
| `·` 가운뎃점 | 5-23 | 제50항. 핵심 |
| `『` 여는 겹낫표 | 56-236 | 제49항 |
| `』` 닫는 겹낫표 | 356-23 | |
| `「` 여는 홑낫표 | 5-236 | |
| `」` 닫는 홑낫표 | 356-2 | |
| `《` 여는 겹화살괄호 | 56-2356 | |
| `》` 닫는 겹화살괄호 | 2356-23 | |
| `〈` 여는 홑화살괄호 | 5-2356 | |
| `〉` 닫는 홑화살괄호 | 2356-2 | |
| `∼` 물결표 (U+223C) | 4-35 | |
| `~` 물결표 (U+007E) | 4-35 | 제55항 (~ 대용) |
| `○` 숨김표 | 456-356-123 (`_0l`) | 제49항. `_`/`l` 묶음 |
| `×` 숨김표 | 456-1346-123 | |
| `△` 숨김표 | 456-346-123 | |
| `□` 빠짐표 | 456-2356-123 | |

### 제14절 기타 부호 (제59항~제73항) — spec.txt L1959~

**신규 추가 필요**

| 묵자 | 규정 | 비고 |
|---|---|---|
| `;` 쌍반점 | 56-23 (`;2`) | 제59항. 현재 `postpunc ; 56-23` 이미 있음 ✓ |
| `※` 참고표 | 5-35 (`"9`) | 제60항. *와 동일 |
| `〃` 상동기호 | 356-356 (`00`) | 제62항 |
| `ː` 긴소리표 | 6-3 (`,'`) | 제63항. 점역자 주표와 동일 점형 |
| `①`~`⑩` 동그라미 숫자 | `#`+(숫자 한 단 내림) | 제64항 |
| `㉠` 등 동그라미 한글 | 7-...-7 | 제64항 |
| 화폐: `￦` `$` `€` `￡` `￠` `₣` `￥` | `0@?` 조합 | 제65항 |
| 점형표 `_=` | 456-123456 | 제67항 |
| 위 첨자 `^` | 45 (`^`) | 제68항. 첨자표 |
| 아래 첨자 `;` | 56 (`;`) | |
| 단위 `%` | 356-1234 | 제69항 [붙임2]. 현재 `always % 356-1234` ✓ |
| 단위 `°` | 356-145 | |
| 단위 `℃` | 356-145-6-14 | |
| 단위 `℉` | 356-145-6-124 | |
| 단위 `‰` | 356-1234-134 | |
| 단위 `′` 분/피트 | 356-36 | |
| 단위 `″` 초/인치 | 356-36-36 | |
| 단위 `Å` 옹스트롬 | 356-16 | |
| `→` 오른쪽 화살표 | 25-135 | 제70항 |
| `←` 왼쪽 화살표 | 246-25 | |
| `↔` 양쪽 화살표 | 246-25-135 | |
| `↓` 아래쪽 화살표 | 45-25-135 | |
| `↑` 위쪽 화살표 | 56-25-135 | |
| `@` 골뱅이 | 4-1 | 제71항. 현재 `always @ 4-1` ✓ |
| `&` 앰퍼샌드 | 4-12346 | 현재 `always & 4-12346` ✓ |
| `§` 섹션 | 45-234 | |
| `¶` 문단 | 45-1234 | |
| `©` 저작권 | 45-14 | |
| `®` 등록상표 | 45-1235 | |
| `™` 상표 | 45-2345 | |
| `\|` 세로선 | 456-1256 | 현재 `always \| 456-1256` ✓ |
| `•` 글머리 가운뎃점 | 456-256 | 제72항 |

### 제12절 숫자와 함께 사용된 기호 (제45~48항) — spec.txt L1522~

| 묵자 | 규정 | 현재 ko.cti | 메모 |
|---|---|---|---|
| `+` 덧셈 | 26 (`5`) | `midnum ^ 45`, `always + 5-235` | 묵자 + ↔ 점자 매핑 재검토 필요 |
| `-` 뺄셈 | 35 (`9`) | `midnum - 36` | 뺄셈은 35; 붙임표(하이픈)는 36 — 구분 필요 |
| `×` 곱셈 | 16 (`*`) | `midnum * 5-23` | |
| `÷` 나눗셈 | 34-34 (`//`) | (없음) | |
| `=` 등호 | 25-25 (`33`) | `always = 25-25` ✓ | |
| `>` 크다 | 26-26 (`55`) | `always > 46-2` | |
| `<` 작다 | 35-35 (`99`) | `always < 5-13` | |
| `1,234` 자릿점 쉼표 | 2 (`1`) | `midnum , 5` | 숫자 사이 쉼표는 2 (자릿점), 일반 쉼표는 5 |
| `1.5` 소수점 | 256 (`4`) | `decpoint . 3` | 현재 잘못됨 |
| `2/3` 분수 빗금 | 34 (`/`) | (없음) | 일반 빗금 _/과 다름 |

### 제6절 약자 (제13~17항) — spec.txt L703~
- 단음절 약자(가-나-다-마-바-사-자-카-타-파-하 등) 자체는 2006판과 동일.
  현재 ko-chars.cti / ko-g2-rules.cti에 이미 반영되어 있을 가능성 높음 → 회귀 테스트로 검증.

### 제7절 약어 (제18항) — spec.txt L813~
- 그래서/그러나/그러면/그러므로/그런데/그리고/그리하여 — 2006판과 동일.

### 제1~제5절 (자모) — spec.txt L477~664
- 기본 자모·받침·모음 점형은 2006판과 동일 (확인 완료).

### 후순위 (별도 작업)
- 제8~9절 옛 글자, 제10절 로마자/그리스, 수학·과학·음악·외국어 점자.

## 결정/미결 질문

- ✅ **결정 (2026-05-03)**: 작업 완료 후 `ko-2006*` 구 규정 테이블은 모두 삭제. (Phase 4에서 처리)
  - 대상: `tables/ko-2006.cti`, `tables/ko-2006-g1.ctb`, `tables/ko-2006-g2.ctb`,
    `tests/braille-specs/ko-2006-g2_harness.yaml`
- ✅ **결정 (2026-05-03)**: 우선순위는 기호·문장부호(제12·13·14절) → 약자/약어 순.
- _Q_: 현재 ko.cti에는 `;` `:` `?` 등이 영어식 매핑(예: `postpunc ; 56-23`은 영어용처럼
  보이지만 우연히 한국 쌍반점과 일치)으로 들어가 있음. 변경 시 영어 텍스트 처리에
  부작용이 없는지 확인 필요.
- _Q_: `ko-g1.ctb`에 영문 약자 처리(`include en-us-g2.ctb`)가 들어가 있는데, 이는
  G1 규정상 부적절할 수 있음. 별도 검토.

## 진행 상황 (날짜순, 최신이 위)

### 2026-05-03
- Phase 0 완료: Poppler 설치, `spec.txt` 추출 (320KB), 절별 행 인덱스 매핑.
- Phase 1 진행: 제12·13·14절(기호·문장부호) 변경점 카탈로그 1차 작성.
  - 제13절 문장 부호: 7개 매핑 수정 + 14개 신규 추가 식별.
  - 제14절 기타 부호: 화폐·단위·화살표·점역자 기호 등 30+ 항목.
  - 제12절 숫자 기호: 연산·비교 기호 9개 매핑 정정 필요.
- 사용자 결정 반영: `ko-2006*` 구 테이블은 작업 완료 후 삭제.
- Phase 2/3 1차: `tables/ko.cti` 문장 부호 영역 갱신.
  - 매핑 정정: `! 235`, `… 6-6-6`, `... 256-256-256`, `— 36-36`,
    `~ 4-35`, `* 5-35`, `^ 4-26`, `_ 456-36`, `\\ 456-16`,
    `( ) [ ] { }` 점형 재배치, `< > +` 수학 부등호로 정정,
    midnum `, → 2` (자릿점), `decpoint . → 256`, `$ → 356-4-234`.
  - 신규 추가: `·`, `『 』`, `「 」`, `《 》`, `〈 〉`, `∼`, `〃`,
    `ː`, 화살표(→ ← ↔ ↑ ↓), `§ ¶ © ® ™`, `•`.
  - 테스트 하네스: `tests/braille-specs/ko-g2_harness.yaml`에 제13·14절
    예제 25개 추가. (실 실행은 liblouis 빌드 후 사용자 측에서 검증 필요.)
- 미수행: liblouis Windows 빌드 부재로 lou_checkyaml 실 실행 불가.
  사용자가 빌드한 환경에서 `lou_checkyaml` 돌려 회귀/실패 케이스 확인 필요.
- 다음 단계:
  1. liblouis 빌드 결과로 테스트 통과/실패 확인 → 실패 케이스 디버깅
  2. 화폐·단위·동그라미 숫자 등 잔여 기호 (제64·65·67·68·69항)
  3. 약자·약어 영역(제6·7절) 회귀 검증 후 미세 조정

## 완료 작업

_아직 없음._
