# CLAUDE.md — 한국어 점자 테이블 2024 개정 작업

이 문서는 **2024년 개정 한국 점자 규정**(문화체육관광부 고시 제2024-0005호)에
맞춰 liblouis의 한국어 테이블을 갱신하는 다중 세션 프로젝트의 작업 기록이다.
대화가 초기화되어도 여기서부터 이어서 작업할 수 있도록 유지한다.

## 작업 원칙

- **작은 단위로 검증 가능하게**: 영역(절) 단위로 변경 → 테스트 추가 → 커밋.
- **회귀 방지**: 변경 전 현재 동작을 테스트로 캡처한 뒤 갱신한다 (실패 → 수정 → 통과).
- **테이블 직접 일괄 수정 금지**: 변경점이 명확히 식별된 항목만 수정. 추측으로
  수천 줄을 건드리지 않는다.
- **진행 상황은 이 파일에 누적 기록**: 완료한 작업은 "완료" 섹션으로 옮기고,
  결정·미결 질문은 해당 섹션에 남긴다.
- **작업 단위마다 커밋**: 논리적으로 한 단위가 끝날 때마다 즉시 커밋한다.
  문서·계획 변경과 코드 변경은 별도 커밋. 한 절(節)·한 카테고리 단위로 분리.
  커밋 메시지는 한국어로, 제목은 영문 prefix(`ko:`, `docs(ko):` 등) 사용.
- **주석 최소화**: 코드가 보여주는 WHAT은 주석으로 반복하지 않는다. WHY가
  비자명할 때만 한 줄. 줄별 한국어 라벨(`# 소괄호 ( )`)은 노이즈이므로 금지.
  규정 항(`# 제45항 …`) 인용 주석도 코드/하네스에 넣지 않는다 — 컨텍스트는
  커밋 메시지로 남긴다.
- **장기 가이드는 메모리 대신 CLAUDE.md에 누적**: 이 프로젝트의 결정·규칙은
  세션 간 메모리 파일이 아닌 본 문서에 기록한다.

## 참고 자료 위치

- 규정 본문 PDF: `C:\braille\[개정]+한국+점자+규정+전문.pdf` (7.6 MB)
- 규정 본문 HWPX: `C:\braille\[개정]+한국+점자+규정+전문.hwpx`
  - 추출본: `C:\braille\hwpx_extracted\Contents\section1.xml` (8.8 MB, 본문 전체)
- 규정 점자 BRF (제1·2권): `C:\braille\한국+점자+규정+개정+전문(점자)\`
- 추출 텍스트(생성 예정): `C:\braille\spec.txt` ← `pdftotext` 결과

## 환경/도구

- OS: Windows 11, PowerShell 7
- 설치된 도구: `scoop`, `winget`, Python 3.14 (`py`, `python`), **Poppler** (`pdftotext` 25.12.0)
- 추출 명령: `pdftotext -layout -enc UTF-8 'C:\braille\[개정]+한국+점자+규정+전문.pdf' 'C:\braille\spec.txt'`

## 점자 글꼴 디코드 (필수)

PDF 본문은 **점자 글꼴**로 인쇄되어, ASCII 문자 = 점 번호(NABCC 매핑) 형태로 보인다.
spec.txt에서 점자 예시(`@s"o` 등)를 해석할 때 사용한다.

| 자모/기호 | ASCII | 점 번호 |
|---|---|---|
| ㄱ 첫소리 | `@` | 4 |
| ㄴ 첫소리 | `c` | 14 |
| ㄷ 첫소리 | `i` | 24 |
| ㄹ 첫소리 | `"` | 5 |
| ㅁ 첫소리 | `e` | 15 |
| ㅂ 첫소리 | `^` | 45 |
| ㅅ 첫소리 | `,` | 6 |
| ㅇ 첫소리 | (생략) / `g`로 표시 시 1245 | (다만2) |
| ㅈ 첫소리 | `.` | 46 |
| ㅊ 첫소리 | `;` | 56 |
| ㅋ 첫소리 | `f` | 124 |
| ㅌ 첫소리 | `h` | 125 |
| ㅍ 첫소리 | `d` | 145 |
| ㅎ 첫소리 | `j` | 245 |
| ㄱ 받침 | `a` | 1 |
| ㄴ 받침 | `3` | 25 |
| ㄷ 받침 | `9` | 35 |
| ㄹ 받침 | `1` | 2 |
| ㅁ 받침 | `5` | 26 |
| ㅂ 받침 | `b` | 12 |
| ㅅ 받침 | `'` | 3 |
| ㅇ 받침 | `7` | 2356 |
| ㅈ 받침 | `k` | 13 |
| ㅊ 받침 | `2` | 23 |
| ㅋ 받침 | `6` | 235 |
| ㅌ 받침 | `8` | 236 |
| ㅍ 받침 | `4` | 256 |
| ㅎ 받침 | `0` | 356 |
| ㅏ | `<` | 126 |
| ㅑ | `>` | 345 |
| ㅓ | `s` | 234 |
| ㅕ | `:` | 156 |
| ㅗ | `u` | 136 |
| ㅛ | `+` | 346 |
| ㅜ | `m` | 134 |
| ㅠ | `%` | 146 |
| ㅡ | `[` | 246 |
| ㅣ | `o` | 135 |
| ㅐ | `r` | 1235 |
| ㅔ | `n` | 1345 |
| 된소리표 | `,` (앞쪽) | 6 |
| 온표 | `=` | 123456 |
| 수표 | `#` | 3456 |

(나머지 NABCC 매핑은 `tables/braille-patterns.cti` 참고.)

## 대상 파일

| 파일 | 크기 | 역할 |
|---|---|---|
| `tables/ko.cti` | 작음 | 기본 헤더, 문장 부호, indicator |
| `tables/ko-chars.cti` | 388 KB | 한글 음절(자모/완성형) → 점자 매핑 |
| `tables/ko-g1-rules.cti` | 633줄 | G1: 수 뒤 한글 처리 (간격 dot 0 삽입) |
| `tables/ko-g2-rules.cti` | 277 KB | G2: 약자/약어/축약, 수 뒤 한글 처리 |
| `tables/ko-g1.ctb`, `ko-g2.ctb` | 작음 | 위 파일들을 묶는 진입점 |
| `tests/braille-specs/ko-g2_harness.yaml` | — | 자동 테스트 |

`tables/ko-2006*.{cti,ctb}` 와 `tests/braille-specs/ko-2006-g2_harness.yaml`
는 Phase 4에서 삭제 예정.

## spec.txt 절별 위치 인덱스 (행 번호)

| 절 | 시작 행 | 비고 |
|---|---|---|
| 한국 점자 표기의 기본 원칙 | ~421 | |
| 제1절 첫소리로 쓰인 자음자 | 477 | |
| 제2절 받침으로 쓰인 자음자 | 513 | |
| 제3절 모음자 | 559 | |
| 제4절 단독으로 쓰인 자모 | 587 | |
| 제5절 모음 연쇄 | 664 | |
| **제6절 약자** | 703 | G2 영향 큼 |
| **제7절 약어** | 813 | G2 영향 큼 |
| 제8절 옛 자음자 | 864 | |
| 제9절 옛 모음자와 방점 | 1045 | |
| 제10절 국어 문장 안의 로마자/그리스 문자 | 1126 | |
| 제11절 국어 문장 안의 숫자 | 1436 | |
| 제12절 숫자와 함께 사용된 기호 | 1522 | |
| **제13절 문장 부호** | 1592 | 사용자 우선순위 |
| **제14절 기타 부호** | 1959 | 사용자 우선순위 |
| 수학 점자 (제1~9장) | 4796~ | 후순위 |
| 음악 점자 (정간보·오선보) | 4796~6386 | 후순위 |
| 외국어 점자 표기 | 8617~ | 후순위 |

## 단계별 계획

- [x] **Phase 0** 환경 준비: Poppler 설치, `spec.txt` 추출, 절별 행 인덱스.
- [~] **Phase 1** 변경점 카탈로그: 제12·13·14절, 제64·65·67·69항 완료.
      자모/약자/약어/옛글자/로마자/수학/음악/외국어 영역 미착수.
- [~] **Phase 2** 테스트 하네스: 제13·14절 25개, 제45·65·67·69·64항 약 25개
      추가. **빌드 환경 미구비** — `lou_checkyaml` 실행해 회귀/실패 확인 필요.
      G1 하네스는 미작성.
- [~] **Phase 3** 영역별 수정: 제13·14절(문장부호·기타), 제45항(연산 기호 ×, ÷),
      제65항(화폐), 제67항(점형표), 제69항[붙임2](비로마자 단위), 제64항(동그라미
      숫자) `ko.cti` 갱신 완료. 잔여: 제64항 동그라미 한글/로마자, 네모 문자,
      제68항(첨자), 제6·7절(약자·약어 회귀 검증), 옛글자/로마자/수학/음악.
- [ ] **Phase 4** 정리: `ko-g1.ctb`/`ko-g2.ctb` 헤더 TODO 정리, 저작권 갱신,
      `ko-2006*` 및 `ko-2006-g2_harness.yaml` 삭제.
- [ ] **Phase 5** (막바지) 언어 전환 마커, 쌍시옷·예 충돌 점검, 한소네 입력
      테이블 신설 — 아래 "막바지 작업" 섹션 참고.

## 미구현 변경점 카탈로그

### 제12절 잔여 (제45~48항)

- `-` 뺄셈: spec은 35지만, 묵자에서 붙임표와 구분 불가 → ko.cti는 36 유지 (의도).
- `*` 곱셈 fallback: 현재 `midnum * 16`, `always * 5-35`. ASCII `*`를 ×로 받을지
  검토. (Unicode × 16은 추가 완료.)
- 분수 빗금 `/`: 제47항 본문 빗금(34, 분모/분수표/분자 순)과 [붙임] 묵자식 빗금
  (`_/` = 456-34) 구분. 현재 `midnum / 456-34`, `always / 456-34`로 [붙임]만 처리.
  본문 분수 사용 시 입력 형식 결정 필요.

### 제64항 잔여 — 동그라미 음절 G1 분리·네모 문자

- 동그라미 한글 음절 ㉮~㉻: 현재 ko-chars.cti에 약자 적용 G2 형태로 등록됨
  (3ab40732). G1에서는 약자 없이 풀어쓰기여야 하지만 같은 출력이 나옴 →
  필요 시 ko-g1-rules.cti에 grade-specific override 추가.
- 네모 문자 (전부): `_8…0l` 형식 (456-236 … 356-123). Unicode 매핑 명확하지
  않음 — 표준 입력 형식을 사용자가 정의해야 적용 가능.

### 제68항 첨자

- 위 첨자: 묵자에 첨자가 나오면 `45` 뒤에 첨자 내용. 일반 ASCII `^` 입력은
  ko.cti에 `always ^ 4-26`로 정의되어 있어 충돌. 입력 형식 결정 필요.
- 아래 첨자: `56` 뒤에 첨자 내용. ASCII `;` 입력은 `postpunc ; 56-23`이라
  충돌.
- 단위 결합: ㎡ → `0m^#b` (단위 m + 위첨자 표시 45 + 숫자 2). 단일 문자
  매핑으로 처리 가능: `always ㎡ 356-134-45-3456-12`. ㎏·㎜·㎝·㎞·㎒·㏊ 등
  공통 SI 단위 문자도 동일 패턴.

### 제69항 잔여 — 추가 단위·결합 처리

- ㎎/㎗ 같은 복합 단위(slash로 연결): 현재 단위 매핑이 각각 0...4로 wrapping
  되므로 "㎎/㎗"는 0mg4 + _/ + 0dl4 = 4셀 wrapping 두 번. spec(0mg_/dl4)은
  바깥 wrapping 한 번. 컨텍스트 규칙으로 인접 단위의 종료/시작 표 제거 필요.
- 미반영 단위: ㎍(μg, 그리스 문자 포함), ㎯, ㎰ 등 잘 안 쓰는 SI 결합. 후속.

### 약자/약어 (제6·7절)

2006판과 동일. ko-chars.cti / ko-g2-rules.cti 기존 매핑 회귀 테스트 후 확인.

### 후순위

제8~9절 옛 글자, 제10절 로마자/그리스, 수학·과학·음악·외국어 점자.

## 막바지 작업 (Phase 5)

다른 영역 작업 마무리 후 마지막 단계로 진행. 외부 사용자 피드백 반영분.

### A. 한글↔로마자 전환 마커 (제30·32항)

- 현재: `ko.cti` L256에 `letsign 356`만 있음. 두 글자 이상 영단어 시작에 356,
  종료에 256 자동 삽입은 **미구현**.
- 가능 여부: **가능**. `attribute hangul …` + `noback context` 조합. `tables/bg.utb`
  L245~272가 Cyrillic↔Latin 전환에 같은 메커니즘 사용 — 패턴 차용 가능.
- 구현 위치 후보: `ko.cti` 또는 별도 `ko-roman-transition.cti`.
- 주의: 기존 `letsign 356`과 상호작용 (한 글자만 = letsign, 두 글자 이상 =
  begin/end markers). lookahead로 분기하거나 letsign 비활성화.
- 한계: 순방향 전용. 역변환은 별도 처리.

### B. 쌍시옷 받침 vs. 예 충돌 (제14항·제16항 영역)

- spec 사실: ㅆ받침 = 점 34, ㅖ "예" = 점 34 (동일 점형).
- spec disambiguation: 제14항 본문(자음자 약자 + 모음 → 약자 미사용),
  **제14항 [붙임]** "팠은 ㅏ를 생략하지 않는다" — **ㅍ만 명시 예외**.
  ㄴ·ㄷ·ㅁ·ㅂ·ㅈ·ㅋ·ㅌ·ㅎ에 대한 [붙임] 명시는 spec.txt에 없음.
- 사용자 언급 "36점 부가" 규칙은 spec.txt 검색 안 됨 — **출처 확인 필요**
  (점역 지침서 등 외부 자료?). 결정되면 `ko-g2-rules.cti`에 컨텍스트 규칙 추가.
- 별개 버그: `ko-g2-rules.cti`의 `always 나예 14-126-34` 류는 제11항
  (모음+예 사이 구분표 36) 누락. 약자/약어 회귀 검증 시 일괄 점검.

### C. 한소네 8점 입력 backward 테이블 (신규)

- 목적: 한소네에서 8점 점자 시퀀스로 입력한 문장부호를 텍스트로 역변환.
- 가능 여부: **가능**. `nofor pass2 @236-38 @238` 형식으로 다중 셀 → 단일 셀
  축약. 동일 패턴이 `tables/th-comp8-backward.utb`에 이미 사용됨.
- 신설 파일: `tables/ko-hansone-comp8-backward.utb`
  - 헤더에 `#+direction: backward` 명시
  - 모든 규칙에 `nofor` 접두사
  - 약 50개 항목 (사용자가 제공한 한소네 18.2.1 한글 문장부호 매핑 표)
- 주의: prefix 충돌 방지를 위해 longest-match 설계. dot 7·8은 한소네
  펌웨어가 엔터/백스페이스로 해석하므로 점자 셀로는 입력 안 됨 (테이블 범위 밖).

## 결정/미결 질문

- ✅ **결정 (2026-05-03)**: 작업 완료 후 `ko-2006*` 구 규정 테이블은 모두 삭제. (Phase 4에서 처리)
  - 대상: `tables/ko-2006.cti`, `tables/ko-2006-g1.ctb`, `tables/ko-2006-g2.ctb`,
    `tests/braille-specs/ko-2006-g2_harness.yaml`
- ✅ **결정 (2026-05-03)**: 우선순위는 기호·문장부호(제12·13·14절) → 약자/약어 순.
- _Q_: 현재 ko.cti에는 `;` `:` `?` 등이 영어식 매핑(예: `postpunc ; 56-23`은 영어용처럼
  보이지만 우연히 한국 쌍반점과 일치)으로 들어가 있음. 변경 시 영어 텍스트 처리에
  부작용이 없는지 확인 필요.
- _Q_: `ko-g1.ctb`에 영문 약자 처리(`include en-us-g2.ctb`)가 들어가 있는데, 이는
  G1 규정상 부적절할 수 있음. 별도 검토.
- _Q_ **(외부 피드백, 2026-05-04)**: 자음자 약자(ㄴ·ㄷ·ㅁ·ㅂ·ㅈ·ㅋ·ㅌ·ㅎ)에서
  쌍시옷 받침/예 충돌을 "36점 부가"로 회피한다는 관행의 출처. spec 본문에는
  없음 — 점역 지침서 등 외부 자료 인용 확인 필요.

## 진행 상황 (날짜순, 최신이 위)

### 2026-05-04
- 외부 피드백 3건 접수 → "막바지 작업" 섹션에 등록:
  (A) 한글↔로마자 전환 마커, (B) 쌍시옷·예 충돌 처리, (C) 한소네 8점 입력 표.
  liblouis 구현 가능성 조사 완료. 모두 **가능** 판정.
- 제69항 SI 단위 문자 21개 추가 (ℓ ㎎ ㎏ ㎜ ㎝ ㎞ ㎘ ㎗ ㎐~㎓ ㎟~㎦ ㏊) (84f9fb7c).
- 제64항 동그라미 한글 음절 ㉮~㉻ 갱신 (3ab40732).
  G1/G2 분리는 후속 — 현 매핑은 약자 적용 형태.
- 제45항 곱셈·나눗셈 기호 (×=16, ÷=34-34) (6c3d3eb4).
- 제65항 화폐 기호 (₩ € ￡ ￠ ₣ ￥) 추가, `begnum $ 256` → always-only 보정 (f0bddd13).
- 제69항 [붙임2] 비로마자 단위 기호 (%, %p, %ile, ‰, °, ℃, ℉, ′, ″, Å) (4d3bd77d).
- 제67항 점형표 `_=` (80f8653c).
- 제64항 동그라미 숫자 ①~⑳ (511b6252).
- 제64항 동그라미 로마자 ⓐ~ⓩ (64367f40).
- 제64항 동그라미 한글 자음 ㉠~㉭ ko-chars.cti 갱신 — 1셀 → 4셀 (28c76db1).
- 누적 회귀 테스트 약 35개. **빌드 환경 미구비** 상태로 검증 불가.
- 다음: 빌드 환경 구성 후 `lou_checkyaml` 실행 → 회귀 확인. 이후 동그라미
  한글 음절(㉮~㉻, G1/G2 분리 필요), 네모 문자, 제68항 첨자, 제69항 SI 단위
  문자 (㎡ ㎏ ㏊ 등).

### 2026-05-03
- Phase 0 완료. Poppler 설치, `spec.txt` 추출, 절별 행 인덱스.
- 제13·14절 문장 부호·기타 부호 `ko.cti` 갱신 (커밋 f4ae9bc1).
  테스트 하네스에 예제 25개 추가.
