# CLAUDE.md — 한국어 점자 테이블 2024 개정 작업

이 문서는 **2024년 개정 한국 점자 규정**(문화체육관광부 고시 제2024-0005호)에 맞춰 liblouis의 한국어 테이블을 갱신하는 다중 세션 프로젝트의 작업 기록이다. 대화가 초기화되어도 여기서부터 이어서 작업할 수 있도록 유지한다.

## 작업 원칙

- **작은 단위로 검증 가능하게**: 영역(절) 단위로 변경 → 테스트 추가 → 커밋.
- **회귀 방지**: 변경 전 현재 동작을 테스트로 캡처한 뒤 갱신한다 (실패 → 수정 → 통과).
- **테이블 직접 일괄 수정 금지**: 변경점이 명확히 식별된 항목만 수정. 추측으로 수천 줄을 건드리지 않는다.
- **하네스 케이스는 spec 예문 우선**: 새 테스트 작성·기존 테스트 갱신 시 해당 항에 등장하는 spec 예문을 그대로 사용. spec 예문이 없거나 모두 너무 길어 단일 케이스로 부적합한 경우에만 최소한의 인공 케이스를 만든다. 인공 케이스는 spec 표(예: 약자 매핑, 부호표) 자체를 검증하는 목적으로만 허용.
- **진행 이력은 `git log`로**: 본 문서에는 살아 있는 결정·규칙·잔여 이슈만 누적. 날짜별 작업 로그는 커밋 메시지로 충분.
- **작업 단위마다 커밋**: 논리적으로 한 단위가 끝날 때마다 즉시 커밋한다. 문서·계획 변경과 코드 변경은 별도 커밋. 한 절(節)·한 카테고리 단위로 분리. 커밋 메시지는 한국어로, 제목은 영문 prefix(`ko:`, `docs(ko):` 등) 사용.
- **주석 최소화**: 코드가 보여주는 WHAT은 주석으로 반복하지 않는다. WHY가 비자명할 때만 한 줄. 줄별 한국어 라벨(`# 소괄호 ( )`)은 노이즈이므로 금지. 규정 항(`# 제45항 …`) 인용 주석도 코드/하네스에 넣지 않는다 — 컨텍스트는 커밋 메시지로 남긴다.
- **장기 가이드는 메모리 대신 CLAUDE.md에 누적**: 이 프로젝트의 결정·규칙은 세션 간 메모리 파일이 아닌 본 문서에 기록한다.
- **커밋은 묻지 말고 바로**: 작업 단위 끝나면 자동 커밋. 다음 작업 착수 전 확인만 받는다. push는 명시 요청 시에만.
- **필요 시 이전 채팅 참고**: 본 문서는 multi-session 요약·결정만 담는다. 시도/회귀의 세부 로그, 사용자 지적의 원문 맥락, 명령 출력 등은 기록하지 않으니, 모호하거나 누락된 컨텍스트는 이전 대화 로그를 확인한다 (예: 특정 커밋의 동기, 되돌린 시도의 회귀 양상, 사용자 지정 옵션의 이유).

## 참고 자료 위치

- 규정 본문 추출 텍스트: `spec.txt` (319 KB) — 절별 행 인덱스는 아래 표 참조.

## 환경/도구

- OS: Windows 11, PowerShell 7
- **빌드 환경**: MSYS2 UCRT64 (`C:\msys64\msys2_shell.cmd -ucrt64`). 필수 패키지: `mingw-w64-ucrt-x86_64-{gcc,python,pkgconf,libyaml}`.
  - 표준 형식: `C:/msys64/msys2_shell.cmd -ucrt64 -defterm -here -no-start -c "..."`
  - 빌드: `./autogen.sh && ./configure --enable-ucs4 && make -j$(nproc)`
  - 회귀 실행 (PowerShell):
    ```
    $env:PATH = "C:\msys64\ucrt64\bin;" + $env:PATH
    $env:LOUIS_TABLEPATH = "C:\liblouis\tables"
    & "C:\liblouis\tools\.libs\lou_checkyaml.exe" "C:\liblouis\tests\braille-specs\ko-g2_harness.yaml"
    ```
  - DLL 우선순위 주의: Windows가 `C:\Windows\System32\liblouis.dll`(접근성용)을 갖고 있으므로, `cp liblouis/.libs/liblouis.dll tools/.libs/`로 exe 옆에 둬야 우리 DLL이 로드됨.

## 점자 글꼴 디코드 (필수)

PDF 본문은 **점자 글꼴**로 인쇄되어 ASCII 문자가 점 번호(NABCC 매핑)를 표현. spec.txt 디코드 시 사용한다.

| 자모/기호 | ASCII | 점 번호 |
|---|---|---|
| ㄱ 첫소리 | `@` | 4 |
| ㄴ 첫소리 | `c` | 14 |
| ㄷ 첫소리 | `i` | 24 |
| ㄹ 첫소리 | `"` | 5 |
| ㅁ 첫소리 | `e` | 15 |
| ㅂ 첫소리 | `^` | 45 |
| ㅅ 첫소리 | `,` | 6 |
| ㅇ 첫소리 | (생략) / `g`로 표시 시 1245 | (다만2) |
| ㅈ 첫소리 | `.` | 46 |
| ㅊ 첫소리 | `;` | 56 |
| ㅋ 첫소리 | `f` | 124 |
| ㅌ 첫소리 | `h` | 125 |
| ㅍ 첫소리 | `d` | 145 |
| ㅎ 첫소리 | `j` | 245 |
| ㄱ 받침 | `a` | 1 |
| ㄴ 받침 | `3` | 25 |
| ㄷ 받침 | `9` | 35 |
| ㄹ 받침 | `1` | 2 |
| ㅁ 받침 | `5` | 26 |
| ㅂ 받침 | `b` | 12 |
| ㅅ 받침 | `'` | 3 |
| ㅇ 받침 | `7` | 2356 |
| ㅈ 받침 | `k` | 13 |
| ㅊ 받침 | `2` | 23 |
| ㅋ 받침 | `6` | 235 |
| ㅌ 받침 | `8` | 236 |
| ㅍ 받침 | `4` | 256 |
| ㅎ 받침 | `0` | 356 |
| ㅏ | `<` | 126 |
| ㅑ | `>` | 345 |
| ㅓ | `s` | 234 |
| ㅕ | `:` | 156 |
| ㅗ | `u` | 136 |
| ㅛ | `+` | 346 |
| ㅜ | `m` | 134 |
| ㅠ | `%` | 146 |
| ㅡ | `[` | 246 |
| ㅣ | `o` | 135 |
| ㅐ | `r` | 1235 |
| ㅔ | `n` | 1345 |
| 된소리표 | `,` (앞쪽) | 6 |
| 온표 | `=` | 123456 |
| 수표 | `#` | 3456 |

(나머지 NABCC 매핑은 `tables/braille-patterns.cti` 참고.)

## 대상 파일

| 파일 | 크기 | 역할 |
|---|---|---|
| `tables/ko.cti` | 작음 | 기본 헤더, 문장 부호, indicator |
| `tables/ko-chars.cti` | 388 KB | 한글 음절(자모/완성형) → 점자 매핑 |
| `tables/ko-g1-rules.cti` | 633줄 | G1: 수 뒤 한글 처리 (간격 dot 0 삽입) |
| `tables/ko-g2-rules.cti` | 277 KB | G2: 약자/약어/축약, 수 뒤 한글 처리 |
| `tables/ko-g1.ctb`, `ko-g2.ctb` | 작음 | 위 파일들을 묶는 진입점 |
| `tests/braille-specs/ko-g2_harness.yaml` | — | 자동 테스트 |

## spec.txt 절별 위치 인덱스 (행 번호)

| 절 | 시작 행 | 비고 |
|---|---|---|
| 한국 점자 표기의 기본 원칙 | ~421 | |
| 제1절 첫소리로 쓰인 자음자 | 477 | |
| 제2절 받침으로 쓰인 자음자 | 513 | |
| 제3절 모음자 | 559 | |
| 제4절 단독으로 쓰인 자모 | 587 | |
| 제5절 모음 연쇄 | 664 | 제11항(예 구분표), 제12항(애 구분표) |
| **제6절 약자** | 703 | G2 영향 큼. 제13·14·15·16·17항 |
| **제7절 약어** | 813 | G2 영향 큼 |
| 제8절 옛 자음자 | 864 | |
| 제9절 옛 모음자와 방점 | 1045 | |
| 제10절 국어 문장 안의 로마자/그리스 | 1126 | 제29·32항 (전환 마커) |
| 제11절 국어 문장 안의 숫자 | 1436 | |
| 제12절 숫자와 함께 사용된 기호 | 1522 | 제45·46·47·48항 |
| **제13절 문장 부호** | 1592 | 사용자 우선순위 |
| **제14절 기타 부호** | 1959 | 제64·65·67·68·69·70·71·72항 |
| 수학 점자 (제1~9장) | 4796~ | 후순위 |
| 음악 점자 (정간보·오선보) | 4796~6386 | 후순위 |
| 외국어 점자 표기 | 8617~ | 후순위 |

## 단계별 계획

- [x] **Phase 0** 환경 준비: Poppler 설치, `spec.txt` 추출, 절별 행 인덱스.
- [~] **Phase 1** 변경점 카탈로그: 제5·12·13·14절, 제6·7절 약자/약어, 제8·9절 옛글자(제19~22·25항), 제64·65·67·69항 완료. 로마자 전환(제29·32~35항) Phase 5 A로 이연. 수학·음악·외국어 미착수.
- [~] **Phase 2** 테스트 하네스: ko-g2 297건(xfail 0) + BrailleSense backward 48건. MSYS2 UCRT64 빌드 환경 구축 완료, 전건 통과(0 failures). G1 하네스는 미작성.
- [~] **Phase 3** 영역별 수정: 아래 "구현 진행" 표 참조.
- [x] **Phase 4** 정리: `ko-2006*` 파일·하네스 삭제, 빌드 시스템 정리, ctb 헤더 TODO/FIXME 제거, 저작권 갱신.
- [ ] **Phase 5** (막바지) 언어 전환 마커, 쌍시옷·예 충돌 점검, 한소네 입력 테이블 신설 — 아래 "막바지 작업" 섹션 참고.

## 구현 진행 (2024 spec 기준)

| 항목 | 상태 | 메모 |
|---|---|---|
| 제11항 모음+예 구분표 36 | ✅ | 252개 엔트리 일괄 보정 (4558c787) + 가예~가옣 28개 추가 |
| 제14항 [붙임] 팠 ㅏ 생략 금지 | ✅ | 145-34 → 145-126-34 |
| 제16항 [붙임] 껐 매핑 정정 | ✅ | 6-456-234-3(된소리+것+ㅅ) → 6-4-234-34(꺼+ㅆ) |
| 제17항 성/썽/정/쩡/청 | ✅ | spec 일치 확인, 변경 없음 |
| 제15항 약자 단음절 14개 | ✅ | spec 일치 확인 (noback always). 하네스는 8366d8bf에서 spec 예문(억새·언어·…)으로 교체 |
| 제15항 [붙임] 표본 (덕/꺾/얹/얽) | ✅ | spec 일치. 하네스 spec 예문(덕망·꺾다·얹다·얽다)으로 교체 |
| 제16항 까/싸 단음절 | ✅ | spec 일치. 하네스 spec 예문(까치·싸리나무)으로 교체 |
| 제13·14절 문장부호·기타 | ✅ | f4ae9bc1 |
| 제45항 ×, ÷ | ✅ | 6c3d3eb4 |
| 제64항 동그라미 숫자 ①~⑳ | ✅ | 511b6252 |
| 제64항 동그라미 로마자 ⓐ~ⓩ | ✅ | 64367f40 |
| 제64항 동그라미 한글 자음 ㉠~㉭ | ✅ | 28c76db1 |
| 제64항 동그라미 한글 음절 ㉮~㉻ | △ | 약자형으로 통일 등록(3ab40732). G1 분리 미해결 |
| 제64항 네모 문자 | ❌ | Unicode 매핑 표준 없음 |
| 제65항 화폐 (₩ € ￡ ￠ ₣ ￥) | ✅ | f0bddd13. `begnum $ 256` → always-only |
| 제67항 점형표 `_=` | ✅ | 80f8653c |
| 제68항 첨자 (위/아래) | ❌ | ASCII `^`/`;`와 충돌. 입력 형식 결정 필요 |
| 제69항 비로마자 단위 (%, °, ℃ 등) | ✅ | 4d3bd77d |
| 제69항 SI 단위 (㎏ ㎡ ㏊ 등) | ✅ | 84f9fb7c 21개 추가. 빗금 인접 단위 wrapping 정리(64247757) — pass2 cell 룰 |
| 제53항 줄임표 (… ...) | ✅ | … → 6-6-6, ... → 256-256-256. 영문 G1 override 차단(56477875) |
| 제6·7절 약자/약어 회귀 검증 | ✅ | 단음절·[붙임]·풀어쓰기 spec 일치 확인, 54건 하네스 추가. 제18항 [다만]은 context 옵코드(d88b067e)로 해소 |
| 제8·9절 옛 글자 (호환자모) | △ | 호환자모 30종(옛 자음자/연서/병서/모음자) + 방점 2종 spec 일치 재확인(제19~21·25항 + 제27항). 순수 매핑은 완료. 잔여=제23항(단독 자음자 _받침)·제24항(ㅏ 생략 금지)·제26항(딴이 ㅣ) — 모두 문맥/입력표현 의존, 검증형 아님 |
| 제19·20·21항 NFD 자모 (첫소리/받침) | ✅ | ᅀ·ᅌ·ᅙ·ᇫ·ᇰ·ᇹ 6건 신규, ᄛ·ᄝ·ᇢ·ᄫ·ᇦ·ᄬ·ᅗ·ᄔ·ᅘ 9건 옛 글자표 5 누락 정정. NFD 표준 단독 15건 하네스 |
| 제22항 NFD 합용병서 초성 | △ | spec 표 12종(16c0a6ab) + 비-표 24종(eb44b40b) 옛글자표 5 정정 = 36종 spec 일치, 호환 11종 일치, NFD 단독 하네스. ᄷ는 counter pass3(ab63c78d)로 spec 값 복원. ㅇ계 12·미등록 5는 spec 근거 없어 보류 (아래 잔여 이슈) |
| 제25항 NFD 옛 모음자 | ✅ | 호환 ㆍ~ㆌ 8종 spec 일치. 결합형 ᆞ(U+119E)·ᆡ(U+11A1) 2건 신규, ᆄ·ᆅ·ᆈ·ᆑ·ᆒ·ᆔ 6건 옛글자표 456 누락 정정. NFD 단독 8건 하네스 (7da3520c) |
| 제10절 로마자/그리스 (제29~36항) | △ | letsign 356 spec 복원(b2b79a10). 다중-letter 그룹 letsign/종료표·영자 블록 내 UEB 부호·제33~35항 예외는 Phase 5 A로 이연 |
| 수학·음악·외국어 | ❌ | 후순위 |

## 알려진 잔여 이슈

### 하네스 비-spec 케이스 (의도적 유지)

spec 예문이 없거나 spec 표(약자·부호·옛글자) 자체 검증용 인공 케이스:

- **제11항 가예~가옣 시리즈**: 제14항 충돌 검증용 인공 케이스(가/나/하/파 + 예), spec 예문 없음.
- **제13·14절 짧은 부호 단독**: 끝., 왜?, 야!, "예", '예', (가), [가], 음…, 음..., €10, 1‰, _=, @a, _가_ 등.
- **제64항 표 등록 검증**: ⑤·⑩·⑳·ⓩ·㉧·㉭·㉯~㉻ 등 단독.
- **제19·20·21·25항 옛 자모자**: NFD/호환 단독 블록 (spec 표 자체 검증).
- **2000년, unity**: 숫자·영문 단독 케이스 (spec 짧은 대응 예문 없음).

### 가 약자 + 모음 시리즈 (제14항 해석 주의)

- 제14항 본문은 **'나, 다, 마, 바, 자, 카, 타, 파, 하'**만 명시 — **가는 제외**. 따라서 가에 모음이 붙어도 **약자(1246) 그대로 사용** (예: `가옥 = $x = 1246+옥`).
- 그러나 가예~가옣은 제11항(모음+예 → 구분표 36)이 적용되어야 함. 현재 동작은 1246+34(예)로 36 누락 → 제11항 위반.
- 가예~가옣 28개 엔트리는 1246-36-34 패턴으로 추가 (풀어쓰기 아님).

### 제68항 첨자 입력 형식

- 위 첨자 `^`(45) / 아래 `;`(56)이 ASCII 입력 그대로면 ko.cti의 `always ^ 4-26`, `postpunc ; 56-23`과 충돌. 입력 표기 결정 필요(예: `^`-회피 또는 별도 마커).

### `ko-g1.ctb`의 영문 약자

- ko-g1.ctb가 `include en-us-g2.ctb` 한 적 없음 (현 시점). g1-rules가 G2 약자를 끌어오지는 않음. 단, ko-g2-rules.cti는 `include en-us-g2.ctb`.

### 영문 G1 부호 override (case-by-case 처리 중)

- 로드 순서: ko.cti → ko-g2-rules.cti(en-us-g2.ctb 포함→ en-us-g1.ctb). 영문 G1이 마지막 → ko.cti의 한국 부호 매핑이 후행 정의에 override될 수 있음. spec 제32항으로 영문 G2 include 자체는 유지 필요.
- **방침**: 충돌 발견된 항목만 ko-g2-rules.cti의 en-us-g2.ctb include 직후에 한국 spec 매핑/counter pass 재선언 (옵션 C). 처리 완료: `...`/`..."`/`...'`(56477875), ᄷ pass2(ab63c78d).
- 근본 개편(ko.cti 분리·include 순서 변경)은 미적용 — 영향 범위·검증 비용 큼.
- en-us → UEB 전환은 막바지 작업 D 참조 (2026-06-08 단순 교체 불가 확인, US 유지).

### 제22항 비-표 합용병서 결합형 초성 (ㅇ계·미등록 잔여)

- spec 제22항은 **일반 규칙**: "합용병서로 만들어진 옛 자음자가 첫소리로 쓰일 때에는 옛 글자표를 앞세워 각 자음자를 어울러 적는다." ㅲ~ㅾ 12종은 예시 표일 뿐 닫힌 목록 아님 → 모든 합용병서가 제22항 적용 대상.
- **완료 (2026-05-17, eb44b40b)**: spec 표 12종(16c0a6ab) + 비-표 24종 `5-` prefix 정정 = 36종 spec 일치. NFD 단독 하네스 누적.
- **ᄷ(U+1137 SIOS-CHIEUCH) 충돌 해소 (ab63c78d)**: en-us pass2가 `5-6-56` → `5-56-6`으로 망가뜨림. ko-g2-rules.cti counter pass3로 원형 복원, 규칙값 spec 일치(`5-6-56`). 36종 중 `6`+`56` 인접은 ᄷ 유일.
- **잔여 (보류, spec 근거 없음)**: ㅇ계 12종(ᄵ·ᅁ~ᅋ·ᅍ·ᅆ) — ㅇ초성 점자 표현이 테이블 자체 불일치(ᅁ=`4` ㅇ누락 vs ᄵ=`6-2356` ㅇ을 2356)이고 spec 제22항 예시에 ㅇ계 없음 → 임의 도출 금지. 미등록 비-ㅇ 5종(ᅚ·ᅛ·ᅜ·ᅝ·ᅞ) — 부재가 결함인지 의도적 비출현인지 불명, 신규 추가 보류.
- 주의: 각자병서(제21항)·연서(제20항)·단독 옛 자음자(제19항)와 혼동 금지. 분류는 Unicode CHOSEONG 이름으로 식별.

## 막바지 작업 (Phase 5)

다른 영역 작업 마무리 후 마지막 단계로 진행. 외부 사용자 피드백 반영분. **작업 종료 시 이 섹션은 삭제**한다 (CLAUDE.md를 짧게 유지).

### A. 한글↔로마자 전환 마커 (제29·32·33·34·35항)

**관련 spec 조항** (spec.txt L1232~1340):
- 제29항: 영자 그룹 앞에 로마자표 0(=356), 뒤에 종료표 4(=256). [다만] 문단 전체가 로마자일 때 시작/종료표 생략 가능 — 강제 아님.
- 제31항: 그리스 문자도 동일 처리.
- 제32항: 로마자표·종료표 사이는 통일영어점자(UEB) 규정 적용 — **영자 부분의 문장부호는 UEB form**.
- 제33항: 영자↔한글 사이 점형이 다른 부호(`, : ; ―`) → 종료표 생략 + 부호는 한글식.
- 제33항 [다만]: `. ? ! ...` 뒤 종료표 생략 / `/ - ~` 앞에 종료표 유지.
- 제34항: 따옴표·괄호 안 영자 → 종료표 생략.
- 제35항: 영자+숫자 → 종료표 생략 (예: `MP3 → 0,,mp#c`).

**현재 동작 진단 (2026-05-28 검토)**:

A1. **단일 letter letsign**: b2b79a10에서 `letsign 356` 재선언으로 spec 일치. 단, en-us-g2.ctb의 `largesign a 1` 등 largesign 매칭은 엔진의 insertLetterSign 경로를 우회해 letsign 미삽입 — 엔진 동작상 spec 위반 아니지만 'a' 단독은 ⠁만 출력.

A2. **다중 letter 그룹 letsign/종료표**: 엔진 자동 처리 없음. 다음 케이스 모두 마커 0건:
| 입력 | 현재 | spec |
|---|---|---|
| `가a가` | ⠫⠁⠫ | ⠫⠴⠁⠲⠫ |
| `가hi가` | ⠫⠓⠊⠫ | ⠫⠴⠓⠊⠲⠫ |
| `안녕 hi 2025` | ⠣⠒⠉⠻ ⠓⠊ ⠼⠃⠚⠃⠑ | ⠣⠒⠉⠻ ⠴⠓⠊⠲ ⠼⠃⠚⠃⠑ (또는 제35항으로 종료표 생략) |

A3. **영자 블록 안 문장부호 (제32항 위반)**:
| 입력 | 현재 | UEB | 일치? |
|---|---|---|---|
| `hi,` | ⠓⠊⠐ (5, 한글식) | ⠓⠊⠂ (2, UEB) | ✗ |
| `hi?` | ⠓⠊⠦ (236, 한글식) | ⠓⠊⠹ (1456, UEB) | ✗ |
| `hi;` | ⠓⠊⠰⠆ (56-23, 한글식) | ⠓⠊⠆ (23, UEB) | ✗ |
| `hi:` | ⠓⠊⠐⠂ (5-2, 한글식) | ⠓⠊⠒ (25, UEB) | ✗ |
| `hi.` | ⠓⠊⠲ (256) | ⠓⠊⠲ (256) | ✓ 우연 일치 |
| `hi!` | ⠓⠊⠖ (235) | ⠓⠊⠖ (235) | ✓ 우연 일치 |

원인: ko.cti의 `postpunc , 5` (한글식) 등이 en-us-g1.ctb의 `postpunc , 2` (UEB)와 공존하지만 매칭 순서상 한글식이 우세. 언어 상태(영자 블록 안인지 밖인지)를 인식할 메커니즘이 없음.

**이전 시도 (2026-05-04)와 회귀 — 참고**:
```
noletsign abcdefghijklmnopqrstuvwxyz
noback context `[]$l @356
noback context $s[]$l @356      # 주: $s는 space, $S가 sign (d88b067e 발견)
noback context $S[]$l @356
noback context $p[]$l @356
noback context $l[]~ @256
noback context $l[]$s @256
noback context $l[]$S @256
```
- 회귀: `unity` → 시작 356 (제29항 [다만] 미반영 — 단 spec은 생략 "가능"만), `@a` 7점 변형, `1 %p` → 단독 'p'가 context에 잡혀 multi-char 매핑 분해, `iPhone` 끝 256 누락(contraction `ne` 흡수).

**다음 세션 작업 계획 (2026-05-28)**:

1. **letsign 그룹 삽입** — context 룩어헤드를 다중 letter 한정(예: `_!$l[]$l$l.`). 단 `%p`(2-char) 같은 multi-char `always` 매핑이 단독 letter 매치에 깨지지 않도록 룩어헤드 자릿수를 `$l2-`로 명시. `correct` 옵코드 시도 후보(pass1 전 단계라 contraction 영향 안 받음).
2. **종료표 256 삽입** — `_$l[]!$l` 변형. 다만 [붙임/다만] 부호별 분기 필요.
3. **제33항 부호별 분기** — `, : ; ―` 직전에는 종료표 생략, `. ? ! ...` 동일 점형이라 그대로, `/ - ~` 앞에는 종료표 유지. 영자→부호→공백/한글 패턴별 별도 context.
4. **제34·35항 예외** — 따옴표/괄호 안, 숫자 인접 → 종료표 생략. 후처리 또는 별도 context.
5. **영자 블록 내 부호 UEB form** — `, : ; ?` 등에 context 추가: `$l[","]$l @2` (letter 사이 콤마 → UEB). ko.cti 기존 한글식 postpunc는 letter 컨텍스트가 아닐 때만 적용되도록.
6. **largesign letsign 누락** — `a` 단독에 letsign이 안 붙는 문제. en-us의 `largesign a 1`을 ko-g2 모드에서 무력화하거나 context로 letsign 강제 삽입 검토.
7. **하네스 추가** — 가a가/가hi가/안녕 hi 2025/hi,bye/KTX, 새마을호/MP3/Ms.는/What Is A Youth?이다/KTX/새마을호/U-도서관 등 spec 예문(L1243~1340) 전수.

**엔진 메커니즘 노트** (2026-05-28 분석):
- `context` 옵코드: pass1 단계에서 입력 char별 매칭. `_` lookback, `!` 부정, `$X` 속성 매칭(`l`=letter, `s`=space, **`S`=sign**), `["..."]` 소비 구간, `@dots` 출력 dots. 빈 소비(`[]`)로 마커 삽입 가능.
- `match` 옵코드: regex-like 패턴. `%[^_]` 등 attribute 클래스. **somewhat experimental** 표기 있음(doc L2091).
- `correct` 옵코드: pre-translation 단계, 입력 문자열 수정. contraction 영향 안 받음 — letsign/termsign 마커를 source 단계에 미리 삽입하는 데 유망.
- pass2/3/4: 출력 dot 스트림 후처리. 언어 식별 어려움.

**한계**: 순방향 전용. 역변환은 별도 처리.

### B. 쌍시옷 받침 vs. 예 충돌 (제14·16항 영역)

- spec 사실: ㅆ받침 = 점 34, ㅖ "예" = 점 34 (동일 점형).
- spec disambiguation: 제14항 본문(자음자 약자 + 모음 → 약자 미사용), **제14항 [붙임]** "팠은 ㅏ를 생략하지 않는다" — **ㅍ만 명시 예외**. ㄴ·ㄷ·ㅁ·ㅂ·ㅈ·ㅋ·ㅌ·ㅎ에 대한 [붙임] 명시는 spec.txt에 없음.
- 사용자 언급 "36점 부가" 규칙은 spec.txt 검색 안 됨 — **출처 확인 필요** (점역 지침서 등 외부 자료?). 결정되면 `ko-g2-rules.cti`에 컨텍스트 규칙 추가.

### C. BrailleSense 8점 입력 backward 테이블 ✅

`tables/ko-braillesense-comp8-backward.utb` 신설 완료. 매핑 본문은 테이블 파일이 권위 — 추가/수정은 거기서.

### D. 영어 약자 테이블 en-us → UEB 전환 (제28·32항)

**동기**: spec 제28·30·32항이 모두 "통일영어점자(UEB) 규정에 따라"를 명시. 현재 `ko-g2-rules.cti:27`은 `en-us-g2.ctb`(EBAE 미국식)를 include — spec과 표준 불일치. UEB가 사실상 표준이므로 `en-ueb-g2.ctb`로 가는 게 목표.

**2026-06-08 시도 결과 — 단순 교체는 불가, US 유지로 되돌림**:
- `en-ueb-g2 → en-ueb-g1`이 **`numericmodechars .,`로 UEB numeric mode를 전역 ON**(en-us엔 없음). 한국어 수 처리(제11절 수 뒤 한글 `after digit` 수백 룰·수표·단위)는 numeric mode OFF를 전제 → 구조적 양립 불가. `numericmodechars`는 테이블당 전역 boolean이고 **끄는 directive가 없다**.
  - 증상: `100㎎` → ㎎ undefined(`'\x338e'`), `100 ㎎`(공백) → 정상. 숫자 뒤 문자가 numeric mode에 흡수됨.
- 추가 충돌: en-ueb-g1이 **en-ueb-math.ctb를 include**(제45항 `+ = < > ÷` 덮음), UEB 부호형(`~ % °` 등), `nonumsign 56`(단독 letter 앞 `⠰` 삽입), emphasis(italic/bold `after`형 — ko.cti와 충돌해 컴파일 에러).
  - 하네스 영향: en-us 0 실패 → en-ueb 18 실패(수학 5·`~` 2·단위/화폐 undefined 8·단독 letter 2·`2000년` 1).
- **결론**: en-us-g2가 깨끗하게 embed됐던 건 "단순"해서(numeric mode·math·공격적 재정의 없음)다. en-ueb는 중심 기능(수·수학·단위)을 깨뜨린다.

**향후 선택지 (택1)**:
1. **현행 유지(en-us)** — 약자는 EBAE. 제32항 'UEB'와 어긋나나 차이는 라틴 블록 내부로 국한. (현재 상태)
2. **UEB 약자만 추린 커스텀 include** — en-ueb-g2를 포크해 `numericmodechars`·en-ueb-math include·충돌 부호/emphasis 제거, 약자·letter·caps만 남긴 ko 전용 테이블 작성. UEB 약자 확보 + 회귀 회피, 단 포크 유지보수 부담.
3. **자체 UEB 구현** — 비현실적(약 4500줄 재현 + 유지보수).
- 어느 길이든 emphasis는 별도 숙제: 영어 g2를 include하는 한 emphasis가 그 영어값으로 전역 고정 → **제56항(드러냄/밑줄 `6-36…36-3`, 굵게 `56-36…36-23`) 불가**. 현재 ko.cti emphasis는 en-us 복사본이라 제56항과 이미 불일치.

날짜별 작업 이력은 `git log`를 참조한다. 본 문서는 살아 있는 결정·규칙·이슈만 누적한다.
